<UserControl x:Class="GUI.Views.NhapHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}" Loaded="UserControl_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📥 Nhập hàng" Style="{StaticResource HeaderLabel}"/>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="4">
                    <TextBlock Text="Mã phiếu nhập" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtMaPN" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="4">
                    <TextBlock Text="Nhà cung cấp" FontSize="12" Foreground="#666"/>
                    <ComboBox x:Name="cboNCC" DisplayMemberPath="TenNCC" SelectedValuePath="MaNCC"
                              FontSize="14" Padding="8,6" Margin="0,4"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="4">
                    <TextBlock Text="Sản phẩm" FontSize="12" Foreground="#666"/>
                    <ComboBox x:Name="cboSanPham" DisplayMemberPath="TenSP" SelectedValuePath="MaSP"
                              FontSize="14" Padding="8,6" Margin="0,4"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="4">
                    <TextBlock Text="Số lượng" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtSoLuong" Style="{StaticResource ModernTextBox}" Text="1"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.Row="1" Margin="4">
                    <TextBlock Text="Đơn giá nhập" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtDonGia" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" Orientation="Horizontal"
                            VerticalAlignment="Bottom" Margin="4,8,0,0">
                    <Button Content="➕ Thêm SP" Style="{StaticResource BtnPrimary}" Click="BtnThemSP_Click"/>
                    <Button Content="❌ Xóa SP" Style="{StaticResource BtnDanger}" Click="BtnXoaSP_Click"/>
                    <Button Content="✅ Lưu phiếu nhập" Style="{StaticResource BtnSuccess}" Click="BtnLuuPhieu_Click"/>
                    <Button Content="🔄 Phiếu mới" Style="{StaticResource BtnAccent}" Click="BtnPhieuMoi_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgChiTiet" Margin="0,8,0,8"/>
        <DataGrid Grid.Row="3" x:Name="dgLichSu" FontSize="12"/>
    </Grid>
</UserControl>
