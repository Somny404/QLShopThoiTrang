<UserControl x:Class="GUI.Views.NhanVienView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}" Loaded="UserControl_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👨‍💼 Quản lý nhân viên" Style="{StaticResource HeaderLabel}"/>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="4">
                    <TextBlock Text="Mã NV" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtMaNV" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="4">
                    <TextBlock Text="Tên NV" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtTenNV" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="4">
                    <TextBlock Text="Giới tính" FontSize="12" Foreground="#666"/>
                    <ComboBox x:Name="cboGioiTinh" FontSize="14" Padding="8,6" Margin="0,4">
                        <ComboBoxItem Content="Nam"/><ComboBoxItem Content="Nữ"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="4">
                    <TextBlock Text="Điện thoại" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtDienThoai" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.Row="1" Margin="4">
                    <TextBlock Text="CMND/CCCD" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtCMND" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Grid.Row="1" Margin="4">
                    <TextBlock Text="Chức vụ" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtChucVu" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Grid.Row="1" Margin="4">
                    <TextBlock Text="Ngày vào làm" FontSize="12" Foreground="#666"/>
                    <DatePicker x:Name="dpNgayVaoLam" FontSize="14" Margin="0,4"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Grid.Row="1" Margin="4">
                    <TextBlock Text="Trình độ" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtTrinhDo" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.ColumnSpan="4" Orientation="Horizontal" Margin="4,8,0,0">
                    <Button Content="➕ Thêm" Style="{StaticResource BtnPrimary}" Click="BtnThem_Click"/>
                    <Button Content="✏️ Sửa" Style="{StaticResource BtnAccent}" Click="BtnSua_Click"/>
                    <Button Content="🗑️ Xóa" Style="{StaticResource BtnDanger}" Click="BtnXoa_Click"/>
                    <Button Content="🔄 Làm mới" Style="{StaticResource BtnSuccess}" Click="BtnLamMoi_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgNhanVien" SelectionChanged="DgNhanVien_SelectionChanged"/>
    </Grid>
</UserControl>
