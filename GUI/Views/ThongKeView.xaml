<UserControl x:Class="GUI.Views.ThongKeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}" Loaded="UserControl_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="📈 Thống kê doanh thu" Style="{StaticResource HeaderLabel}"/>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Tháng:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="cboThang" Width="80" FontSize="14" Padding="8,6">
                    <ComboBoxItem Content="Tất cả" Tag="0"/>
                    <ComboBoxItem Content="1"/><ComboBoxItem Content="2"/><ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/><ComboBoxItem Content="5"/><ComboBoxItem Content="6"/>
                    <ComboBoxItem Content="7"/><ComboBoxItem Content="8"/><ComboBoxItem Content="9"/>
                    <ComboBoxItem Content="10"/><ComboBoxItem Content="11"/><ComboBoxItem Content="12"/>
                </ComboBox>
                <TextBlock Text="Năm:" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <TextBox x:Name="txtNam" Width="80" Style="{StaticResource ModernTextBox}"/>
                <Button Content="📊 Xem thống kê" Style="{StaticResource BtnPrimary}" Margin="16,0,0,0"
                        Click="BtnThongKe_Click"/>
                <Button Content="🏆 Top bán chạy" Style="{StaticResource BtnAccent}" Margin="4,0,0,0"
                        Click="BtnTopBanChay_Click"/>
                <Button Content="⚠ SP sắp hết" Style="{StaticResource BtnDanger}" Margin="4,0,0,0"
                        Click="BtnSPSapHet_Click"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgThongKe" AutoGenerateColumns="True"/>
    </Grid>
</UserControl>
