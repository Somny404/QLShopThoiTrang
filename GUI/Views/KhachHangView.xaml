<UserControl x:Class="GUI.Views.KhachHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}" Loaded="UserControl_Loaded">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="👤 Quản lý khách hàng" Style="{StaticResource HeaderLabel}"/>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Margin="4">
                    <TextBlock Text="Mã KH" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtMaKH" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="4">
                    <TextBlock Text="Tên KH" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtTenKH" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="4">
                    <TextBlock Text="Điện thoại" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtDienThoai" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="4">
                    <TextBlock Text="Email" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>
                <StackPanel Grid.Column="4" Margin="4">
                    <TextBlock Text="Địa chỉ" FontSize="12" Foreground="#666"/>
                    <TextBox x:Name="txtDiaChi" Style="{StaticResource ModernTextBox}"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="5" Orientation="Horizontal" Margin="4,8,0,0">
                    <Button Content="➕ Thêm" Style="{StaticResource BtnPrimary}" Click="BtnThem_Click"/>
                    <Button Content="✏️ Sửa" Style="{StaticResource BtnAccent}" Click="BtnSua_Click"/>
                    <Button Content="🗑️ Xóa" Style="{StaticResource BtnDanger}" Click="BtnXoa_Click"/>
                    <Button Content="🔄 Làm mới" Style="{StaticResource BtnSuccess}" Click="BtnLamMoi_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" x:Name="dgKhachHang" SelectionChanged="DgKhachHang_SelectionChanged"/>
    </Grid>
</UserControl>
